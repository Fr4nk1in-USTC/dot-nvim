-- general language config
require("languages.lsp") -- language server protocol
require("languages.dap") -- debug adapter protocol
require("languages.runner") -- code runner
require("languages.format") -- null-ls for formatting
require("languages.hightlight") -- treesitter for hightlight
require("languages.completion") -- nvim-cmp for completion
-- language specific config
require("languages.markdown") -- Markdown support
